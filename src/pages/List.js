import React from "react";
import styled from "styled-components";
import Map from "../components/Map";

const List = (props) => {
  return (
    <React.Fragment>
      <Map />
    </React.Fragment>
  );
};

export default List;
